{
  "name": "pandaroo",
  "private": true,
  "version": "0.1.0-beta.13",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm --workspace shared run build && npm-run-all -p dev:shared dev:backend dev:frontend",
    "dev:shared": "npm --workspace shared run dev",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace shared run build && npm --workspace frontend run build && npm --workspace backend run build",
    "start": "npm --workspace backend run start",
    "test": "npm --workspace backend run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
